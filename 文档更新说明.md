# 文档更新说明

> 更新时间：2026-09-03
> 背景：项目代码已经过多轮迭代（双语支持、主菜单改写、历史/语言问题修复、旧报错修复等），但三份 md 文档还停留在早期版本。本次按当前代码（`script.rpy` / `options.rpy` / `screens.rpy` / `ch01.rpy` / `gui.rpy` / `game/tl/`）逐份核对更新，并顺带清理了工程目录里的不必要文件。
> 核对方式：通读全部源码 + 运行 `renpy.exe lint` 校验。

---

## 一、文件清理（已删除）

经确认删除，均为可再生或已过期的文件（大部分本就已被 `.gitignore` 排除）：

| 删除内容 | 数量/大小 | 理由 |
|----------|-----------|------|
| 全部 `*.rpyc` 编译缓存 | 11 个 | Ren'Py 启动时自动重新生成 |
| `Anti-Fraud Fox/game/cache/` | 4 个文件，约 593K | 字节码缓存，自动生成 |
| `Anti-Fraud Fox/game/saves/` | 21 个文件，约 1.7M | 本机测试存档 + persistent 进度；删除后本机进度重置回第 1 关（已确认） |
| `Anti-Fraud Fox/log.txt` | 1 个 | 运行日志，每次启动覆盖 |
| `Anti-Fraud Fox/errors.txt` | 1 个 | 过期报错：screens.rpy:760 `style_prefix = "radio"` 语法错误，当前代码已改为 `style_prefix "radio"`，问题不存在 |
| `Anti-Fraud Fox/traceback.txt` | 1 个 | 过期崩溃：about 界面 `NameError: name 'Assets' is not defined`，当前 about 界面已无 `Assets` 引用，问题不存在 |

**经确认保留**：`打开游戏.txt`（启动命令备忘）、`问题.txt`（历史问题记录，两个问题当前版本均已修复）、`game/images/` 全部图片（含暂未引用的 `clear.png`、`logo.jpg`、`l1_final.png` 及各角色无表情底图，后续会用到）。

---

## 二、《反诈狐项目介绍与技术文档.md》的修改

1. **二、玩法与游戏流程**：原来写「进入游戏后直接是选关界面」，与现状不符。改为完整流程：启动 → 约 2 秒标题加载画面（`label splashscreen`）→ 主菜单（底部 5 按钮）→ 选关（5×3 网格、星级显示）→ 闯关 → 结算；并新增「语言可切换」一条。
2. **三、项目目录结构**：
   - 删掉过期的绝对路径 `C:\Users\25825\Desktop\Anti-Fraud Fox\`，改为相对描述。
   - 目录树按仓库实际内容重画：新增 `game/tl/`（翻译）、`game/audio/说明.md`、根目录 `打开游戏.txt` / `问题.txt` / `.gitignore`；删掉树里的 `log.txt`、`cache/`、`saves/`（它们已不在仓库内）。
   - 原「配套开发目录」表格保留，但明确注明这些目录只在开发机上、不在 Git 仓库内；删掉 `Anti-Fraud Fox.lnk` 快捷方式一行。
   - 新增一段说明：自动生成的 `.rpyc` / `cache/` / `saves/` / 三个日志文件不进仓库，且已于 2026-09-03 清理。
3. **四、核心文件详解**：
   - `script.rpy`：补充 `label splashscreen` 加载画面；补充「旧 5 星制存档启动时压到 3 星以内」；补充选关进关前清空对话历史；角色定义补「白字黑描边」。
   - `options.rpy`：把「`has_sound`/`has_music` 当前未配音频」的旧说法改为「开关已开启、设置界面显示音量条、audio/ 待放音频」；新增 `config.default_language`、老存档语言初始化、`gui.about`、`text_cps = 40`、`build.*` 具体值、`config.console = True` 等条目。
   - `screens.rpy`：自定义界面表从 3 个扩到 5 个（新增改写的主菜单 `main_menu`、带语言切换的设置界面）；更正 `level_clear`/`level_fail` 的描述——结算界面只渲染结算图和按钮，`tip` 参数保留但当前不在界面上渲染（原写法「显示防骗口诀」与代码不符）；新增历史记录空态与清空机制说明；标准界面列表补上 `help`。设置界面的英文显示统一写作 Settings（与游戏内一致；`preferences` 只作为代码屏幕名出现）。
4. **五、表情系统**：原来写 7 种表情所有角色通用，与 `script.rpy` 实际注册不符。改为按角色列出可用表情表（如 `police` 没有 `worried`、`mc` 没有 `suspicious`），并提醒用缺少的表情前需先补图注册。
5. **七、如何新增一关**：新增第 7 步「补英文翻译」（`translate english` 生成骨架后填译文）。
6. **五、ch01 结构说明**：把「口诀结算时显示」更正为「作为 `tip` 参数传给结算界面（目前不单独渲染）」，与 screens 一节保持一致。
6. **八、如何运行/打包**：删掉 `C:\Users\25825\...` 旧绝对路径和 `.lnk` 方式，改为「`<SDK 目录>/renpy.exe` + `<本仓库目录>/Anti-Fraud Fox`」的相对写法，并注明 `打开游戏.txt` 里有现成命令。
7. **新增「九、双语支持」章节**：默认语言、切换位置、`game/tl/` 各文件分工、`_()` / `__()` 包裹规则、新增关卡的翻译流程。原「九、技术要点小结」顺延为「十」，并追加双语、音频两条要点。

## 三、《关卡编写规范.md》的修改

1. **二、文件结构**：表格新增 `game/audio/`、`game/tl/english/` 两行；`screens.rpy` 一行补充「主菜单、设置（含语言切换）」；`script.rpy` 一行补充「加载画面」；`options.rpy` 一行补充「默认语言、音频开关」；`tl/schinese` 注明仅 `common.rpy`。表下提示补充：新增关卡要跑 `translate english` 补 `game/tl/english/chNN.rpy`。
2. **三、3.2 表情属性**：原来只列 7 种属性、暗示全角色通用。新增「各角色已注册立绘」对照表（含每个角色缺哪种表情），并注明用缺少的表情前要先按第七节补图并在 `script.rpy` 注册。
3. **五、关键点 3**：明确 `tip`（防骗口诀）参数目前保留但结算界面不单独渲染，避免写新关卡时以为口诀会显示在结算屏上。
4. **五、关键点 7（语言约定）**：补充第 1 关英文翻译已完成（`tl/english/ch01.rpy`）；明确新关卡不补翻译时英文模式会显示中文原文。
5. **七、素材位置**：删掉过期绝对路径 `C:\Users\25825\Desktop\Anti-Fraud Fox\素材\`，改为「开发机上仓库同级的 `素材/` 目录（不上传 GitHub）」。

## 四、`game/audio/说明.md` 的修改

1. 新增「当前状态」一节：说明 `options.rpy` 声音/音乐开关已开启、设置界面有音量条，但本目录目前还没有音频文件。
2. 使用方式末尾新增一条约定：音频文件名用 ASCII，避免中文文件名影响打包分发（lint 也会对中文文件名报警告）。

---

## 五、顺带发现（未改代码，仅记录）

- `renpy lint` 当前只有一条警告：`game/tl/english/ch01.rpy:373` 英文译文里的 `100% a scam` 被 lint 当作旧式 `%` 格式码。经核对 Ren'Py 运行时只对含 `[` 的字符串做插值，该句不含 `[`，**运行时不会报错**，属无害警告；如需消除可把译文 `%` 改为 `%%`。
- `问题.txt` 记录的两个历史问题在当前版本中均已修复：① 设置里可切换中/英、系统提示已汉化；② 进关卡/重试时会清空 `_history_list`，历史不跨关残留。

---

## 六、第二轮补充修改（2026-09-03）

### 1. 修复英文 about 界面报错（代码/翻译修改）

- **原因**：`game/tl/english/options.rpy` 里 `gui.about` 的英文译文用了半角方括号 `[Assets]` / `[Version]`。Ren'Py 的文本显示会把 `[...]` 当变量插值求值，英文模式打开 about 界面时求值 `Assets` 抛 `NameError` 崩溃（与早前旧 traceback 同类）。
- **修改**：改成全角方括号 `【Assets】` / `【Version】`，与中文原文的【素材来源】/【版本】风格一致，不再触发插值。
- **补译**：about 界面的「使用 Ren'Py x.x 引擎制作」一行此前没有英文翻译（英文模式会显示中文原文），在 `game/tl/english/screens.rpy` 补上：`"\nMade with the {a=https://www.renpy.org/}Ren'Py{/a} [renpy.version_only] engine"`。
- 已排查全部英文翻译，其余 `[...]`（如 `[text]`、`[i]`、`[persistent.unlocked]`、`[renpy.version_only]`）都是引擎合法插值变量，无同类隐患。

### 2. 设置界面英文表述统一为 Settings

- 核对游戏内 4 处 `_("设置")`（快进菜单、游戏菜单导航、主菜单、设置界面标题）英文译文均为 **Settings**，本就一致。
- 文档措辞统一：技术文档「四、4」里原来用代码标识符 `preferences` 当作英文称呼的两处，改为「设置（Settings，代码里屏幕名为 `preferences`）」/「英文显示为 Settings」；`preferences` 今后在文档中只作为代码屏幕名出现（如 `preferences.text_cps` 这类代码变量不受影响）。

### 3. `.gitignore` 补充

- 新增 `errors.txt`、`traceback.txt` 两条。这两个文件是 Ren'Py 运行/报错时自动生成的（验证时发现跑一次 lint 就会重新生成），原先只有 `log.txt` 被忽略，补上后不会再被误提交进仓库。
