# 《反诈狐》项目介绍与技术文档

> 一款面向中小学生的反诈宣传剧情游戏（视觉小说）。
> 本文件介绍这款游戏是做什么的、项目怎么组织的、每个文件分别有什么作用，适合向同学、老师或评委介绍。

---

## 一、游戏简介

- **游戏名**：《反诈狐》
- **引擎**：Ren'Py 8.5.3（一款专门做「视觉小说 / 剧情游戏」的开源引擎）
- **分辨率**：1280 × 720，支持全屏
- **题材**：中小学生反诈教育，动物卡通画风
- **玩家身份**：主角是一只小狐狸「阿狐」

**玩法一句话概括：** 玩家在贴近日常的上网、游戏、消费场景中，面对各类诈骗套路，通过「三选一」做出抉择，走向「反诈成功」或「被骗翻车」的不同结局，在剧情中学会识别诈骗。

**核心原则（贯穿全游戏）：** 陌生链接不点、陌生转账不做、隐私信息不给、遇事必找家长老师警察。

---

## 二、玩法与游戏流程

1. **开场**：启动后先显示约 2 秒的标题加载画面（`script.rpy` 里的 `label splashscreen`），随后进入主菜单。
2. **主菜单**：标题图底部一排按钮——读取进度 / 设置 / 开始游戏 / 关于 / 退出游戏。
3. **选关**：点「开始游戏」进入关卡选择界面，5×3 网格显示 15 个关卡；未解锁的关卡灰色不可点，已解锁的显示「第 N 关 + 关卡名 + 最佳星级」。
4. **闯关**：每关都是「前置剧情 → 三轮三选一抉择」。
   - 每个选择会走向不同的分支结局，有的选择直接通关，有的选择把玩家「引向更深的陷阱」进入下一轮。
5. **结算**：通关显示对应星级的结算图；被骗则显示失败图，可「重新挑战」或「返回选关」。
6. **语言**：设置里可切换「简体中文 / English」，默认为简体中文（见第九节）。

### 关卡清单（15 关诈骗类型）

| 关卡 | 诈骗类型 | 关卡 | 诈骗类型 |
|------|---------|------|---------|
| 1 | 免费皮肤 | 2 | 点赞返利 |
| 3 | 冒充同学 | 4 | 快递中奖 |
| 5 | 网课退费 | 6 | 粉丝福利 |
| 7 | 租号卖号 | 8 | 虚假网购 |
| 9 | 冒充公检法 | 10 | 私密照勒索 |
| 11 | 刷单垫资 | 12 | 校园贷 |
| 13 | 虚假中奖 | 14 | AI换脸冒充 |
| 15 | 终极试炼 | | |

> 当前只有第 1 关「免费皮肤诈骗」完成了剧情，是标准范本；第 2~15 关为占位，后续照第 1 关的结构补齐。

### 星级与解锁机制

- **3 星制**：每个关卡根据玩家抉择的「警惕程度」给 1 / 2 / 3 星（3 星最好）。
- **顺序解锁**：通关任意结局（哪怕 1 星）就自动解锁下一关。
- **被骗结局**：不解锁下一关、不计星，可重试。

---

## 三、项目目录结构

整个项目放在 `Anti-Fraud-Fox-main/`（Git 仓库根目录），里面分「游戏本体」和「配套文档」：

```
Anti-Fraud-Fox-main/                          ← 项目根目录（整个 Git 仓库）
├── Anti-Fraud Fox/                           ← 游戏本体（Ren'Py 工程），核心，必须
│   ├── project.json                          ← 工程配置文件
│   ├── 关卡编写规范.md                        ← 项目内部编写规范
│   └── game/                                 ← 游戏全部内容
│       ├── script.rpy                        ← 角色、图片、进度、路由、加载画面
│       ├── options.rpy                       ← 窗口标题、版本、默认语言、打包等配置
│       ├── gui.rpy                           ← 界面颜色、字号、字体、布局
│       ├── screens.rpy                       ← 各类界面（含主菜单/选关/结算/语言切换）
│       ├── ch01.rpy                          ← 第 1 关剧本（标准范本）
│       ├── images/                           ← 全部图片素材（64 张）
│       ├── audio/                            ← 音频目录（当前暂无音频文件，内含说明.md）
│       ├── font/                             ← 字体文件
│       ├── gui/                              ← 界面皮肤素材
│       └── tl/                               ← 翻译文件（双语支持）
│           ├── schinese/common.rpy           ← 引擎系统提示的简体中文官方翻译
│           └── english/                      ← 英文翻译（界面、系统提示、第 1 关剧情）
├── .gitignore                                ← 不上传 GitHub 的文件清单
├── 反诈狐项目介绍与技术文档.md                 ← 本文档
├── 打开游戏.txt                               ← 启动命令备忘
└── 问题.txt                                   ← 历史问题记录（两个问题均已修复）
```

> 说明：`Anti-Fraud Fox/` 是游戏本体，单独拷走就能运行；运行时自动生成的 `*.rpyc`、`game/cache/`、`game/saves/`、`log.txt`、`errors.txt`、`traceback.txt` 不进仓库（已在 `.gitignore` 中排除），2026-09-03 已把它们从工程目录里清理掉。

### 开发机上的配套目录（不在 Git 仓库内）

以下目录只存在于开发机上，是开发过程中的配套资料，不影响游戏运行，也不上传 GitHub（已在 `.gitignore` 中排除）：

| 目录 | 作用 |
|------|------|
| `素材/` | 原始图片素材（AI 生成原图，抠图前） |
| `音频/` | 原始音频素材（BGM / 音效，处理前） |
| `关卡剧本/` | 策划文档（每关文字剧本） |
| `工具/` | 开发脚本与工具（抠图、改文档、快捷方式等） |
| `测试/` | 测试计划、用例、问题清单（BUG 记录） |
| `文档/` | 设计文档、开发笔记、会议记录归档 |
| `发布版/` | 打包发布产物（体积大，不上传 GitHub） |

> 每个配套目录内都有一个 `说明.md`，写明了该目录的用途和命名规范，开发时照此存放即可。

---

## 四、核心文件详解

### 1. `game/script.rpy` —— 游戏骨架（最重要）

作用：定义角色、注册图片、管理进度、写游戏路由。

- **角色定义**（`define xxx = Character(...)`）：定义了 7 个角色 + 旁白，统一白字黑描边。
- **图片定义**（`image xxx = ...`）：把 `images/` 里的图片注册成可以在剧本里直接用的名字，例如 `bg title`、`l1_msg`、`side mc serious`。
- **加载画面**（`label splashscreen`）：启动时黑场淡入标题图并停留约 2 秒。
- **关卡名称表** `CHAPTER_NAMES`：15 关的中文名称列表，选关界面用它显示。
- **进度持久化** `persistent`：用 Ren'Py 的持久化变量保存「已解锁到第几关」和「每关最佳星级」；旧版本曾是 5 星制，启动时会把历史星级统一压到 3 星以内。
- **通关函数** `clear_chapter(章节, 星级)`：记录最佳星级并解锁下一关。
- **路由**：`label start` 进入选关，`label select_level` 弹选关界面（进关前清空对话历史），然后 `jump chN` 跳到对应关卡。
- **第 2~15 关占位**：每个 `label chN` 目前只显示「开发中」。

### 2. `game/options.rpy` —— 全局配置

作用：设置游戏的基本信息。

- `config.name = _("反诈狐")`：游戏显示名（可翻译）。
- `config.version = "1.0"`：版本号；`gui.about` 里写了「关于」界面展示的介绍与素材来源说明。
- `config.default_language = "schinese"`：首次运行默认简体中文；玩家切换语言后选择会持久保存（老存档另有一段初始化逻辑兜底）。
- `config.save_directory = "fanzha-xiaoweishi"`：**存档目录标识**（固定写死，跟文件夹名无关，改名不影响存档）。
- `config.window_icon = "gui/window_icon.png"`：任务栏/窗口图标。
- `config.has_sound` / `config.has_music = True`：声音/音乐开关已开启，设置界面会显示音量条；目前 `audio/` 目录还没有音频文件，剧本里加 `play music / play sound` 即可生效。
- `preferences.text_cps = 40`：默认文字速度 40 字/秒。
- `build.name = "fanzha"`、`build.directory_name` / `build.executable_name = "反诈狐"`：打包发布时用。
- `config.console = True`：开发期开启调试控制台（Shift+O）。

### 3. `game/gui.rpy` —— 界面主题

作用：设置界面的**颜色、字号、字体、边距、布局尺寸**等视觉参数。

- 字体用的是「思源黑体」（Source Han Sans CN），文件在 `game/font/` 里。
- 对话框、按钮、文字颜色等外观都在这份文件里统一配置。

### 4. `game/screens.rpy` —— 各类界面

作用：定义所有「界面（screen）」。分两类：

**引擎自带的标准界面**（Ren'Py 默认提供，本作做了汉化和微调）：
`say`（对话框）、`choice`（选项菜单）、`main_menu`（主菜单）、`game_menu`（游戏菜单）、`save`/`load`（存档/读档）、设置（Settings，代码里屏幕名为 `preferences`）、`history`（历史）、`about`（关于）、`confirm`（确认框）、`notify`（提示）、`help`（帮助）等。

**本作自定义/改写的界面**：

| 界面 | 作用 |
|------|------|
| `main_menu()` | 主菜单：标题图 + 底部一排按钮（读取进度/设置/开始游戏/关于/退出游戏） |
| `level_select()` | 关卡选择界面（5×3 网格，15 关，锁定的关卡灰色不可点，已解锁的显示星级） |
| `level_clear(chapter, stars, tip)` | 通关结算界面（按星级显示 1/2/3 星通关图 + 返回选关；`tip` 参数保留，当前不在界面上渲染） |
| `level_fail(chapter, tip)` | 被骗结局界面（失败图 + 重新挑战/返回选关；`tip` 同上） |
| 设置界面（代码屏幕名 `preferences`） | 英文显示为 Settings；在标准项之外加了「语言」切换（简体中文 / English） |

> 历史记录界面 `history()` 在没有记录时会显示「对话历史为空。」；每次从选关进入关卡、以及被骗重试时，剧本都会清空 `_history_list`，历史不会跨关残留。

### 5. `game/ch01.rpy` —— 关卡剧本（第 1 关标准范本）

作用：第 1 关「免费皮肤诈骗」的完整剧情脚本，是后面所有关卡照抄的结构模板。

结构：
- `define l1_tip = "..."`：本关的防骗口诀（顺口溜），作为 `tip` 参数传给结算界面（目前界面保留参数、不单独渲染）。
- `label ch1`：前置剧情 + 第一轮三选一。
- 三个分支 `ch1_A`（理智避险，3 星）/ `ch1_B`（好奇试探，进第二轮）/ `ch1_C`（主动上钩，进第三轮）。
- 分支下再分：满分（3 星）、及时止损（2 星）、轻微泄露（1 星）、完全被骗（失败）等结局。
- 每个结局调用 `clear_chapter()` 记录星级 + `call screen level_clear()` 弹结算。

### 6. `game/images/` —— 图片素材（共 64 张）

| 类型 | 命名 | 说明 |
|------|------|------|
| 角色立绘 | `{角色}_{表情}.png`（如 `mom_serious.png`） | 透明底头像，共 6 个角色的 30+ 张表情 |
| 背景图 | `bg_xxx.png` / `title.jpg` | 标题页、卧室、客厅等 |
| 关卡插图 | `l1_xxx.png/jpg` | 第 1 关的手机界面、聊天截图、钓鱼网页等 |
| 结算图 | `clear1/2/3.png`、`fail.png` | 1/2/3 星通关图 + 被骗失败图 |

### 7. `game/font/` —— 字体

- `SourceHanSansCN-Regular.ttf`：思源黑体，开源字体（SIL Open Font License）。

### 8. `game/gui/` —— 界面皮肤素材

作用：界面的边框、按钮、滑块、文本框等皮肤图片（Ren'Py 默认主题皮肤）。

### 9. `project.json` —— 工程配置

作用：告诉 Ren'Py 这是一个游戏工程，记录显示名、构建类型等元信息。

### 10. 自动生成的文件（不需要手改）

- **`.rpyc` 文件**：`.rpy` 脚本的「编译缓存」，Ren'Py 运行时会自动把 `.rpy` 编译成 `.rpyc` 加速启动，删了会自动重新生成。
- **`game/cache/`**：字节码缓存，自动生成。
- **`game/saves/`**：存档文件，玩家存档后生成。
- **`log.txt`**：运行日志，每次启动会覆盖。

---

## 五、角色系统

### 角色清单（在 `script.rpy` 定义）

| 变量 | 显示名 | 形象 | 用途 |
|------|--------|------|------|
| `mc` | 阿狐 | 小狐狸 | 主人公说话 |
| `think` | 阿狐 | 小狐狸 | 主人公内心独白（文字自动加括号） |
| `fraud` | 客服 | 浣熊 | 诈骗分子 |
| `mom` | 妈妈 | 狐狸母亲 | 家长 |
| `dad` | 爸爸 | 狐狸父亲 | 家长 |
| `teacher` | 老师 | 猫头鹰 | 学校 |
| `police` | 反诈警察 | 狗狗警长 | 反诈科普 |
| `n` | （无） | — | 旁白，不显示名字 |

### 表情系统

表情属性共 7 种：`normal`（正常）、`serious`（严肃）、`laugh`（大笑）、`worried`（担心）、`sad`（沮丧）、`confused`（疑惑）、`suspicious`（怀疑）。**不是每个角色都有全部 7 种**，当前各角色已有的表情立绘：

| 角色 | 可用表情 |
|------|----------|
| `mc` 阿狐 | normal、serious、laugh、worried、sad、confused |
| `fraud` 客服 | normal、serious、laugh、worried、sad、suspicious |
| `mom` 妈妈 | normal、serious、laugh、worried、sad、confused |
| `dad` 爸爸 | normal、serious、laugh、worried、sad、confused |
| `teacher` 老师 | normal、serious、laugh、worried、sad、suspicious |
| `police` 反诈警察 | normal、serious、laugh、sad、suspicious、confused |

剧本里这样写：`mom serious "宝贝，这是骗局哦。"` 就会显示妈妈「严肃」表情的立绘。写剧本前请对照上表，用到没有的表情图时需要先补图并在 `script.rpy` 注册。

### 三种说话方式

```renpy
n "旁白，没有名字和头像。"          # 旁白
mc serious "天下没有免费的皮肤。"     # 阿狐说话，左侧显示严肃表情
think worried "总觉得不太对劲。"      # 阿狐内心独白，文字加括号
```

---

## 六、进度与存档机制

- 用 Ren'Py 的 **`persistent` 持久化变量** 保存进度，而不是普通存档，所以「通关记录」在退出游戏后依然保留。
- `persistent.unlocked`：已解锁到第几关（初始为 1，通关后 +1）。
- `persistent.stars`：字典，记录每关最佳星级（只升不降）。
- 存档文件实际存放在系统目录 `%APPDATA%\RenPy\fanzha-xiaoweishi\`（由 `config.save_directory` 决定，跟工程文件夹名无关）。

---

## 七、如何新增一关（开发向）

1. 新建/填充 `game/chNN.rpy`（如第 2 关就是 `ch02.rpy`），照 `ch01.rpy` 的结构写。
2. 顶部写 `define lNN_tip = "本关防骗口诀"`。
3. 用 `label chNN:` 开头，按「前置剧情 → 三轮三选一」写分支。
4. 通关调用 `$ clear_chapter(NN, 星级)` + `call screen level_clear(...)`；被骗调用 `call screen level_fail(...)`。
5. 准备好本关图片，命名 `l{N}_{场景}`，放进 `game/images/`。
6. 跑 `renpy.exe "项目" lint` 检查，再启动游戏验证。
7. 补英文翻译：在启动器或命令行执行 `renpy.exe "项目" translate english` 生成 `game/tl/english/chNN.rpy` 翻译骨架，再填上英文译文（见「九、双语支持」）。

> 更详细的规范见工程内的 `关卡编写规范.md`。

---

## 八、如何运行 / 打包

### 运行游戏

- **方式一（最简单）**：用仓库根目录 `打开游戏.txt` 里的命令（PowerShell 语法）：
  ```
  & "<SDK 目录>/renpy.exe" "<本仓库目录>/Anti-Fraud Fox"
  ```
  例如本机 SDK 与仓库同在 `D:\临时\claude\` 下时，即 `& "D:\临时\claude\renpy-8.5.3-sdk\renpy-8.5.3-sdk\renpy.exe" "D:\临时\claude\Anti-Fraud-Fox-main\Anti-Fraud Fox"`。
- **方式二（启动器）**：双击 SDK 里的 `renpy.exe`，在项目列表选「Anti-Fraud Fox」，点 Launch Project。
- **方式三（命令行）**：
  ```
  renpy.exe "<本仓库目录>/Anti-Fraud Fox"
  ```

> 即：第一个参数是 Ren'Py SDK 的 `renpy.exe`，第二个参数是仓库内层的游戏本体目录 `Anti-Fraud Fox/`。

### 语法检查

```
renpy.exe "<本仓库目录>/Anti-Fraud Fox" lint
```

### 打包发布（生成可双击的 exe）

在 Ren'Py 启动器里选「Build Distributions（构建分发版）」，即可打包成 PC 版本（Windows / Mac / Linux）。

---

## 九、双语支持（简体中文 / English）

- **默认语言**：简体中文（`config.default_language = "schinese"`）；玩家在「设置」界面的「语言」一栏切换，选择会持久保存。
- **翻译文件**在 `game/tl/` 下：
  - `schinese/common.rpy`：引擎系统提示（存读档、设置、帮助等界面文字）的简体中文官方翻译，勿手改。
  - `english/`：英文翻译，含 `common.rpy`（系统提示）、`screens.rpy` / `options.rpy` / `script.rpy`（界面与配置文案）、`ch01.rpy`（第 1 关剧情对话）。
- **界面文字**一律用 `_()` 包裹才能被翻译；动态文本（如关卡名）用 `__()`。
- 新增关卡后，用 `renpy.exe "项目" translate english` 生成英文翻译骨架（`game/tl/english/chNN.rpy`），再补填译文。

---

## 十、技术要点小结

- **引擎**：Ren'Py 8.5.3，脚本语言是它自带的剧本语法（label / scene / show / menu / jump），底层是 Python。
- **分辨率**：1280 × 720，图片用 `Transform(..., xsize=1280, ysize=720)` 强制铺满。
- **立绘**：透明底 PNG，统一高度 1783px（`fraud`、`mom` 例外缩到 1426px），由 `side_portrait` transform 统一控制位置和缩放。
- **素材**：AI 生成，需用 Python（PIL + numpy + scipy）做「白底转透明 + 去水印」预处理。
- **进度**：`persistent` 持久化变量 + `clear_chapter()` 函数。
- **双语**：`game/tl/` 翻译文件 + `_()` / `__()` 包裹界面文字，设置界面切换语言。
- **音频**：`config.has_sound` / `config.has_music` 已开启，`audio/` 目录待放入音频文件。
